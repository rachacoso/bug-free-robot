<%= form_for(@choice_category) do |f| %>
  <% if @choice_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@choice_category.errors.count, "error") %> prohibited this choice_category from being saved:</h2>

      <ul>
      <% @choice_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <div class="row">
      <div class="small-3 columns">
        <%= f.label :name, "Name" %>  
      </div><!--/.small-3-->
      <div class="small-9 columns">
        <%= f.text_field :name %>  
      </div><!--/.small-12-->
    </div><!--/.row-->
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
