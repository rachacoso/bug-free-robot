<%= form_for(@media_type) do |f| %>
  <% if @media_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_type.errors.count, "error") %> prohibited this media_type from being saved:</h2>

      <ul>
      <% @media_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
